# Контекст проекта «Генеалогическое дерево»

> Единый источник правды о проекте. Обновляется при значимых изменениях.
> Последнее обновление: 2026-06-20

## Что это

Интерактивный семейный сайт-родословная для двух родов:
- **Шахназарян** (армянская ветвь)
- **Курудимовы / Мангул** (белорусская и украинская ветвь)

Сайт показывает генеалогическое дерево, галереи фото и документов, исторические
факты, географию рода и «книгу» — оформленную летопись с перелистыванием страниц.
Данные можно редактировать прямо на сайте (карточки людей, загрузка фото и документов).

## Где что лежит

| Что | Где |
|---|---|
| Репозиторий | https://github.com/alionamangul/Genialogy |
| Боевой сайт | https://genialogy.vercel.app |
| Исходник (всё в одном файле) | `index.html` (~5300 строк HTML+CSS+JS) |
| Данные | `data.json` (295 человек, 120 семей, заметки) |
| Медиа | `media/` (фото) и `media/docs/` (документы) |
| Локальный сервер для разработки | `serve.py` (Python, порт 8081) |
| Бэкенд для прода | `api/*.py` (Vercel Python serverless functions) |
| Конфиг Vercel | `vercel.json` |
| Парсер исходного GEDCOM | `parse_ged.py`, `parse_ged.js`, `My.ged` |

## Два режима работы

### 1. Локальная разработка
- `python3 serve.py` поднимает сервер на порту **8081**.
- Статика отдаётся из `/tmp/family-tree/` (песочница Claude не пишет в `~/Downloads`).
- **Важно:** после правки `index.html` или `serve.py` нужно скопировать файл в `/tmp/family-tree/`,
  иначе браузер увидит старую версию.
- Сохранение данных пишет в `/tmp/family-tree/data.json` и пытается продублировать в `~/Downloads`.

### 2. Прод (Vercel)
- Статика (`index.html`, `media/`) отдаётся как есть.
- Редактирование работает через `api/*.py`:
  - `update-data.py` — сохраняет `data.json` в GitHub через GitHub API.
  - `upload-photo.py` / `upload-doc.py` — заливают файл в **Vercel Blob**, а ссылку дописывают в `data.json`.
- Для работы нужны переменные окружения в Vercel:
  - `GITHUB_TOKEN` — токен с правами `repo` (запись в репозиторий).
  - `BLOB_READ_WRITE_TOKEN` — добавляется автоматически при подключении Vercel Blob Storage.

## Совместимость путей к медиа

Старые фото лежат в репозитории как `media/<id>.jpg`, новые — в Vercel Blob как полный URL.
В коде это разводят хелперы:
- `photoUrl(ref)` — если `ref` начинается с `http`/`/`, возвращает как есть; иначе `./media/<ref>.jpg`.
- `docUrl(ref)` — аналогично для `./media/docs/<ref>`.

Поэтому в `data.json` поле `photoIds[]` может содержать и числовой id, и полный URL — оба работают.

## Ключевые люди (корни дерева)

| Человек | ID |
|---|---|
| Армен Шахназарян (корень по умолчанию) | `43938060` |
| Алёна Мангул (супруга) | `7323016` |
| Авинер Шахназарян (отец Армена) | `42972468` |
| Людмила Азарян (мать Армена) | `84381481` |
| Шамир (Иосиф) Азарян (дед по матери) | `95547960` |
| Евгения Оганесова (бабушка по матери) | `60995756` |
| Григорий Шахназарян (дед по отцу) | `13356830` |
| Офелия Данилян (бабушка по отцу) | `37845118` |

> ⚠️ В старом `SESSION_CONTEXT.md` для Алёны указан id `35454522` — это устарело,
> актуальный id `7323016`. При расхождениях верить этому файлу и проверять `data.json`.

## Структура данных (`data.json`)

```jsonc
{
  "individuals": {
    "<id>": {
      "id", "givn", "surn", "secg", "sex",
      "birthDate", "birthPlace", "deathDate", "deathPlace",
      "occupation", "residence", "notes",
      "fams": [],        // семьи, где человек — супруг
      "famc": [],        // семья, где человек — ребёнок
      "photoIds": [],    // id или URL фотографий
      "documents": []    // [{ file, title }]
    }
  },
  "families": { "<id>": { "husb", "wife", "chil": [] } },
  "notes": { ... }
}
```

## Связанные документы

- [FUNCTIONALITY.md](FUNCTIONALITY.md) — что сайт умеет (функциональное описание).
- [ARCHITECTURE.md](ARCHITECTURE.md) — как он устроен (схемы).
- [BACKLOG.md](BACKLOG.md) — задачи на будущее.
